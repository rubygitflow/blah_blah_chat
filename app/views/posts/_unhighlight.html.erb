<%= link_to( '𓇼',
  highlight_link(post),
  data: { "turbo-method": :post },
  class: %w(
    link-secondary
    link-underline-opacity-0
    border-start
    col-md-auto
  ).join(' '),
  id: highlight_id(post),
) %>
