<%= link_to( '⭐',
  highlight_link(post),
  data: { "turbo-method": :post },
  class: %w(
    link-secondary
    link-underline-opacity-0
    border-start
    border-warning
    col-md-auto
  ).join(' '),
  id: highlight_id(post),
) %>
