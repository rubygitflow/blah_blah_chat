<div class='container control-panel sticky-top'>
  <div class='d-flex align-items-center justify-content-between'>
    <div class='fs-1 fw-bold text-warning'>
      Blah_blah_chat
    </div>
    <div>
      <%= link_to 'Add new topic', new_chat_path,
        class: 'btn btn-danger',
        data: { turbo_frame: 'remote_modal' } %>
    </div>
  </div>
</div>
<%= turbo_stream_from('chats') %>
<%= turbo_stream_from 'chat_posts_count' %>
<%= turbo_frame_tag 'chats' do %>
  <%= render @chats %>
<% end %>
<%= render 'chats/next_index_page' %>
<%= render partial: 'shared/streaming_toast' %>
